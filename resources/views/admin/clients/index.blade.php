<x-layouts.app title="Clients" :breadcrumbs="[['label'=>'Content'],['label'=>'Clients']] data-noindex><x-admin.page-header title="Clients" description="Manage factual, administrator-entered canonical content."><x-slot:actions><a class="admin-button-secondary" href="{{ route('admin.clients.create') }}">Create</a></x-slot:actions></x-admin.page-header><x-admin.card class="mt-6"><div class="overflow-x-auto"><table class="w-full min-w-[60rem] text-left text-sm"><thead><tr><th class="p-3">Name</th><th>Status</th><th>Featured</th><th>Order</th><th>Publication</th><th>Updated</th><th>Actions</th></tr></thead><tbody>@forelse($clients as $client)<tr class="border-t"><td class="p-3 font-semibold">{{ $client->name }}</td><td>{{ ucfirst($client->status) }}</td><td>{{ $client->is_featured ? 'Yes' : 'No' }}</td><td>{{ $client->display_order }}</td><td>{{ optional($client->scheduled_for ?: $client->published_at)->format('Y-m-d H:i') ?: '—' }}</td><td>{{ $client->updated_at->format('Y-m-d') }}</td><td><a class="underline" href="{{ route('admin.clients.edit',$client) }}">Edit</a></td></tr>@empty<tr><td colspan="7" class="p-8 text-center">No records configured.</td></tr>@endforelse</tbody></table></div></x-admin.card><div class="mt-6">{{ $clients->links() }}</div></x-layouts.app>