<x-layouts.app title="License diagnostics" :breadcrumbs="[['label' => 'Dashboard', 'url' => route('dashboard')], ['label' => 'License', 'url' => route('license.status')], ['label' => 'Diagnostics']]">
<div class="space-y-6"><x-admin.page-header title="License diagnostics" description="Safe local state only. Tokens, entitlement payloads, keys, and credentials are excluded." /><x-admin.card title="Diagnostics"><dl class="divide-y divide-slate-200 text-sm">@foreach(['Product' => config('naxas-license.product'), 'License type' => $state?->license_type ?? config('naxas-license.license_type'), 'Normalized domain' => $state?->normalized_domain ?? 'Unavailable', 'Status' => $state?->license_status ?? 'Verification required', 'Last verified' => $state?->last_verified_at?->toIso8601String() ?? 'Unavailable', 'Acknowledged' => $state?->acknowledged_at?->toIso8601String() ?? 'Unavailable'] as $label => $value)<div class="grid gap-1 py-3 sm:grid-cols-2"><dt class="text-slate-500">{{ $label }}</dt><dd class="break-words font-semibold">{{ $value }}</dd></div>@endforeach</dl></x-admin.card></div>
</x-layouts.app>
