<x-layouts.app :title="Work Processes" :breadcrumbs="[['label'=>'Content'],['label'=>'Work Processes']]"><x-admin.page-header title="Work Processes" description="Manage lifecycle, ordering, relationships, and public visibility."><x-slot:actions><a class="admin-button-secondary" href="{{ route('admin.work-processes.create') }}">Create</a></x-slot:actions></x-admin.page-header>@if(session('status'))<x-admin.alert class="mt-4">{{ session('status') }}</x-admin.alert>@endif<x-admin.card class="mt-6"><div class="overflow-x-auto"><table class="w-full min-w-[60rem] text-left text-sm"><thead><tr><th class="p-3">Title</th><th>Slug</th><th>Status</th><th>Featured</th><th>Relations / stages</th><th>Order</th><th>Publication</th><th>Updated</th><th>Actions</th></tr></thead><tbody>@forelse($processes as $item)<tr class="border-t"><td class="p-3 font-semibold">{{ $item->title }}</td><td>{{ $item->slug }}</td><td><x-admin.status-badge :status="$item->status"/></td><td>{{ $item->is_featured ? 'Yes':'No' }}</td><td>{{ $item->stages_count }} stages</td><td>{{ $item->display_order }}</td><td>{{ optional($item->scheduled_for ?: $item->published_at)->format('Y-m-d H:i') ?: '—' }}</td><td>{{ $item->updated_at->format('Y-m-d') }}</td><td><a class="underline" href="{{ route('admin.work-processes.edit',$item) }}">Edit {{ $item->title }}</a></td></tr>@empty<tr><td colspan="9" class="p-8 text-center">No records configured.</td></tr>@endforelse</tbody></table></div></x-admin.card><div class="mt-6">{{ $processes->links() }}</div></x-layouts.app>
