<x-layouts.app title="Statistics" :breadcrumbs="[['label'=>'Content'],['label'=>'Statistics']] data-noindex><x-admin.page-header title="Statistics" description="Manage factual, administrator-entered canonical content."><x-slot:actions><a class="admin-button-secondary" href="{{ route('admin.statistics.create') }}">Create</a></x-slot:actions></x-admin.page-header><x-admin.card class="mt-6"><div class="overflow-x-auto"><table class="w-full min-w-[60rem] text-left text-sm"><thead><tr><th class="p-3">Label</th><th>Status</th><th>Featured</th><th>Order</th><th>Publication</th><th>Updated</th><th>Actions</th></tr></thead><tbody>@forelse($statistics as $statistic)<tr class="border-t"><td class="p-3 font-semibold">{{ $statistic->label }}</td><td>{{ ucfirst($statistic->status) }}</td><td>{{ $statistic->is_featured ? 'Yes' : 'No' }}</td><td>{{ $statistic->display_order }}</td><td>{{ optional($statistic->scheduled_for ?: $statistic->published_at)->format('Y-m-d H:i') ?: '—' }}</td><td>{{ $statistic->updated_at->format('Y-m-d') }}</td><td><a class="underline" href="{{ route('admin.statistics.edit',$statistic) }}">Edit</a></td></tr>@empty<tr><td colspan="7" class="p-8 text-center">No records configured.</td></tr>@endforelse</tbody></table></div></x-admin.card><div class="mt-6">{{ $statistics->links() }}</div></x-layouts.app>