@props(['title', 'description' => null, 'state' => 'empty'])
<div {{ $attributes->merge(['class' => 'flex min-h-64 flex-col items-center justify-center rounded-public border border-dashed border-public-border bg-white p-8 text-center']) }}><span class="flex size-12 items-center justify-center rounded-full bg-slate-100 text-xl text-public-muted" aria-hidden="true">{{ $state === 'loading' ? '…' : '○' }}</span><h3 class="mt-4 text-xl font-bold text-public-navy">{{ $title }}</h3>@if($description)<p class="mt-2 max-w-lg leading-7 text-public-secondary">{{ $description }}</p>@endif @isset($action)<div class="mt-5">{{ $action }}</div>@endisset</div>
