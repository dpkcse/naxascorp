@props(['settings' => null, 'siteName', 'legalName', 'navigation' => []])
<footer class="bg-public-navy text-slate-300"><div class="public-container grid gap-10 py-14 md:grid-cols-2 lg:grid-cols-[1.4fr_1fr_1fr]"><div><div class="flex items-center gap-3"><span class="flex size-10 items-center justify-center rounded-lg bg-blue-600 font-bold text-white" aria-hidden="true">N</span><strong class="text-xl text-white">{{ $siteName }}</strong></div><p class="mt-4 max-w-md leading-7">{{ $settings?->tagline ?: 'Premium corporate website foundation for clear, accessible digital communication.' }}</p><p class="mt-4 text-sm">{{ $legalName }}</p></div>
<div><h2 class="font-semibold text-white">Website</h2><ul class="mt-4 grid gap-3 text-sm"><li><a class="public-footer-link" href="{{ route('home') }}">Home</a></li><li><span aria-disabled="true">Company <span class="text-slate-500">— Coming soon</span></span></li><li><span aria-disabled="true">Solutions <span class="text-slate-500">— Coming soon</span></span></li></ul></div>
<div><h2 class="font-semibold text-white">Contact</h2>@if($settings?->primary_email || $settings?->primary_phone || $settings?->country_code)<address class="mt-4 grid gap-3 text-sm not-italic">@if($settings?->primary_email)<a class="public-footer-link break-all" href="mailto:{{ $settings->primary_email }}">{{ $settings->primary_email }}</a>@endif @if($settings?->primary_phone)<a class="public-footer-link" href="tel:{{ preg_replace('/[^+0-9]/', '', $settings->primary_phone) }}">{{ $settings->primary_phone }}</a>@endif @if($settings?->country_code)<span>{{ $settings->country_code }}</span>@endif</address>@else<p class="mt-4 text-sm text-slate-400">Contact details are not configured.</p>@endif</div></div><div class="border-t border-white/10"><div class="public-container flex flex-col gap-2 py-6 text-sm sm:flex-row sm:items-center sm:justify-between"><p>&copy; {{ now()->year }} {{ $legalName }}.</p><p>Developed by Naxas Innovations Limited</p></div></div></footer>
