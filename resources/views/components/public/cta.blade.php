@props(['eyebrow' => null, 'heading', 'description' => null, 'variant' => 'light'])
@php $classes = match($variant) {'dark' => 'bg-public-navy', 'gradient' => 'bg-linear-to-br from-public-navy to-blue-800', default => 'bg-blue-50'}; $dark = $variant !== 'light'; @endphp
<section class="public-section {{ $classes }} {{ $dark ? 'text-white' : 'text-public-text' }}"><div class="public-container"><div class="grid items-center gap-8 lg:grid-cols-[1fr_auto]">@isset($media)<div class="lg:col-span-2">{{ $media }}</div>@endisset<div>@if($eyebrow)<p class="public-eyebrow {{ $dark ? 'text-blue-200' : '' }}">{{ $eyebrow }}</p>@endif<h2 class="mt-3 text-3xl font-bold tracking-tight sm:text-4xl">{{ $heading }}</h2>@if($description)<p class="mt-4 max-w-3xl text-lg leading-8 {{ $dark ? 'text-slate-200' : 'text-public-secondary' }}">{{ $description }}</p>@endif</div><div class="flex flex-wrap gap-3">@isset($primary){{ $primary }}@endisset @isset($secondary){{ $secondary }}@endisset</div></div></div></section>
