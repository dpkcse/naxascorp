@props(['eyebrow' => null, 'title', 'description' => null, 'alignment' => 'center'])
<header {{ $attributes->merge(['class' => ($alignment === 'left' ? 'max-w-3xl' : 'mx-auto max-w-3xl text-center')]) }}>@if($eyebrow)<p class="public-eyebrow">{{ $eyebrow }}</p>@endif<h2 class="mt-3 text-3xl font-bold tracking-tight text-balance text-public-navy sm:text-4xl">{{ $title }}</h2>@if($description)<p class="mt-4 text-lg leading-8 text-public-secondary">{{ $description }}</p>@endif @isset($action)<div class="mt-6">{{ $action }}</div>@endisset</header>
