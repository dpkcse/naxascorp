@props(['name', 'label', 'type' => 'text', 'value' => null, 'helper' => null, 'required' => false, 'disabled' => false])
@php $id = $attributes->get('id', $name); $error = $errors->first($name); $describedBy = collect([$helper ? $id.'-help' : null, $error ? $id.'-error' : null])->filter()->implode(' '); @endphp
<div class="grid gap-2"><label for="{{ $id }}" class="public-label">{{ $label }}@if($required)<span class="text-red-700" aria-hidden="true"> *</span><span class="sr-only"> required</span>@endif</label><input id="{{ $id }}" name="{{ $name }}" type="{{ $type }}" value="{{ old($name, $value) }}" @required($required) @disabled($disabled) @if($describedBy) aria-describedby="{{ $describedBy }}" @endif @if($error) aria-invalid="true" @endif {{ $attributes->except('id')->merge(['class' => 'public-control']) }}>@if($helper)<p id="{{ $id }}-help" class="public-helper">{{ $helper }}</p>@endif @if($error)<p id="{{ $id }}-error" class="public-error">{{ $error }}</p>@endif</div>
