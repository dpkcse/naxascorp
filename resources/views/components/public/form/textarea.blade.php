@props(['name', 'label', 'value' => null, 'helper' => null, 'required' => false, 'rows' => 5])
@php $id = $attributes->get('id', $name); $error = $errors->first($name); @endphp
<div class="grid gap-2"><label for="{{ $id }}" class="public-label">{{ $label }}@if($required)<span class="text-red-700" aria-hidden="true"> *</span><span class="sr-only"> required</span>@endif</label><textarea id="{{ $id }}" name="{{ $name }}" rows="{{ $rows }}" @required($required) @if($error) aria-invalid="true" aria-describedby="{{ $id }}-error" @endif {{ $attributes->except('id')->merge(['class' => 'public-control']) }}>{{ old($name, $value) }}</textarea>@if($helper)<p class="public-helper">{{ $helper }}</p>@endif @if($error)<p id="{{ $id }}-error" class="public-error">{{ $error }}</p>@endif</div>
