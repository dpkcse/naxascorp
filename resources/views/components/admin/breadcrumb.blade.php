@props(['items' => []])
<nav aria-label="Breadcrumb" {{ $attributes }}><ol class="flex min-w-0 items-center gap-2 text-sm">@foreach($items as $item)<li class="flex min-w-0 items-center gap-2">@if(!$loop->last && isset($item['url']))<a class="truncate text-slate-500 hover:text-blue-700 focus-visible:ring-2 focus-visible:ring-blue-600" href="{{ $item['url'] }}">{{ $item['label'] }}</a><span aria-hidden="true" class="text-slate-400">/</span>@else<span class="truncate font-medium text-slate-900" @if($loop->last) aria-current="page" @endif>{{ $item['label'] }}</span>@endif</li>@endforeach</ol></nav>
