@props(['name', 'label', 'help' => null, 'required' => false, 'options' => null, 'value' => null])
<flux:field><flux:label>{{ $label }}@if($required)<span class="text-red-600" aria-hidden="true"> *</span>@endif</flux:label><flux:select :name="$name" :required="$required" {{ $attributes }}>@if(is_array($options))@foreach($options as $optionValue=>$optionLabel)<flux:select.option :value="$optionValue" :selected="(string)old($name,$value)===(string)$optionValue">{{ str_replace('_',' ',ucfirst($optionLabel)) }}</flux:select.option>@endforeach @else{{ $slot }}@endif</flux:select>@if($help)<flux:description>{{ $help }}</flux:description>@endif<flux:error :name="$name" /></flux:field>
